import React from "react";

// (work) route group layout — section header for /projects and /projects/:id
export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
