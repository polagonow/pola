package actions

// [[.Name]] exposes functions and variables to JavaScript via the Pola bridge.
//
// How it works:
//   - Each exported method becomes a callable function in JS
//   - Method names are automatically camelCased: GetItems → [[.Name]].getItems()
//   - Parameters are positional and type-safe (string, int, bool, structs)
//   - Return must be (T, error) or just error
//   - The special Vars() method exposes read-only constants to JS
//
// Usage in React:
//   import { [[.Name]] } from "@pola/actions"
//   const items = await [[.Name]].getAll()
//
// Run "pola generate" to regenerate the bridge after editing this file.

// [[.Name]] is your action struct. Add fields for dependencies (DB, services, etc.)
// that will be resolved via dependency injection.
type [[.Name]] struct {
	// DB *sql.DB  // example: injected database connection
}

// Item is an example type returned by your methods.
// Struct fields map to TypeScript interfaces via json tags.
type [[.Name]]Item struct {
	ID   int    `json:"id"`
	Name string `json:"name"`
}

// GetAll returns all items. Becomes [[.Name]].getAll() in JavaScript.
func (a *[[.Name]]) GetAll() ([]*[[.Name]]Item, error) {
	// TODO: implement
	return nil, nil
}

// GetByID returns a single item by ID. Becomes [[.Name]].getByID(id) in JavaScript.
func (a *[[.Name]]) GetByID(id string) (*[[.Name]]Item, error) {
	// TODO: implement
	return nil, nil
}

// Vars exposes read-only values to JavaScript as properties on the action.
// Each key becomes accessible as [[.Name]].keyName in JS.
//
// func (a *[[.Name]]) Vars() map[string]any {
// 	return map[string]any{
// 		"appName": "My App",
// 	}
// }
