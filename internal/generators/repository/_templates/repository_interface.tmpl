package repositories

import (
	"[[.PolaPackage]]/repository"

	"[[.ModulePath]]/[[.ModelsDir]]"
)

// [[.Name]]Repository defines the contract for [[.SnakeName]] persistence
// operations. The entity type is the canonical model in [[.ModelsDir]] — the
// single source of truth for the schema and the transport entity. It embeds the
// framework's standard CRUD contract; add custom query methods here.
type [[.Name]]Repository interface {
	repository.Repository[ [[.ModelsPkg]].[[.Name]], [[.IDGoType]]]
}
