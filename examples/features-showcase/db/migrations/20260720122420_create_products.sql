-- Create "products" table
CREATE TABLE `products` (`id` integer NULL PRIMARY KEY AUTOINCREMENT, `name` varchar NULL, `price` real NULL, `description` text NULL, `created_at` datetime NULL, `updated_at` datetime NULL);
